#include "ModelManager.h"



ModelManager::ModelManager()
{
}


bool ModelManager::AddModel(char * modelPath)
{
	
	/*if (models.count(modelPath) != 1)
	{
		Model temp(modelPath);
		models.insert(modelPath, temp);
	}*/
	return false;
}

ModelManager::~ModelManager()
{
}
